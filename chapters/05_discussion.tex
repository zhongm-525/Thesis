\chapter{Discussion}
\label{chap:discussion}

\section{Interpretation of results}
\label{sec:disc_interpretation}

The results of Chapter~\ref{chap:results} do not identify a single best inverse-kinematics method. Each method tested dominated some evaluation regime on some dataset, and each was beaten elsewhere. This section reads the results as a coherent story rather than as isolated experiments, argues that the regimes depend on a small number of physical properties of the system, and proposes explanations for the least obvious findings.

\paragraph{Every classical baseline failed, but for different reasons.}
The single-segment PCC of Section~\ref{sec:res_pcc_single} reached a $22$\,mm mean forward residual on the reachable subset of Dataset 2, well above the $\sim$$2$--$10$\,mm scale that would make it a competitive baseline on a workspace roughly $100$\,mm across. The diagnostic analysis attributed this to a dominant elongation signal ($r = +0.737$ between total pressurisation and tip norm) that a constant-arc-length PCC model has no degree of freedom to represent. The two-segment PCC of Section~\ref{sec:res_pcc_multi} reached geometric inverse residuals of $3 \times 10^{-3}$\,mm---essentially zero---yet its closed-loop error on Dataset 1 was $51$\,mm, roughly four times larger than the worst data-driven baseline. Here the failure is different: the geometry is fine, but the multi-valued two-segment inverse hands the linear pressure-calibration step a set of training frames whose chamber lengths collide on very different pressure commands, and ordinary least squares recovers the per-chamber mean pressure. These two failure modes are instructive because they show that a correct geometric model does not guarantee a competitive inverse: the inverse has to be well-posed both in configuration space and in actuation space, and neither dataset provides that joint well-posedness for a pure PCC pipeline.

\paragraph{Data-driven regression lets the inverse be as ill-posed as the dataset allows.}
Direct-regression networks do not need the inverse to be well-posed: when the inverse maps one tip to many pressures, the least-squares loss gently pulls the network toward the training-distribution average pressure for that tip. This is visible on Dataset 1, where the tip-only feed-forward network reached $R^{2} = 0.32$---a value that reflects a genuinely under-determined inverse, not a training failure, as confirmed by the early validation plateau at epoch $4$ (Section~\ref{sec:res_mlp_tip}). On Dataset 2, where three valves map into three tip coordinates, the inverse is essentially single-valued on the observed workspace, and the same architecture reached $R^{2} = 0.93$. This contrast is one direct argument for diagnosing the task by actuation redundancy rather than by architecture choice: the direct-regression target is the per-tip posterior mean over valid pressures, and its predictive ceiling is set by the concentration of that posterior, not by the expressive power of the network.

\paragraph{Mid-marker input lifts the posterior concentration; temporal context amplifies it.}
Adding the mid-marker coordinates to the Dataset 1 input was the largest single-factor improvement reported in this thesis ($R^{2}$ from $0.32$ to $0.44$ on the feed-forward network, from $0.28$ to $0.51$ on the recurrent network; Sections~\ref{sec:res_mlp_tipmid}, \ref{sec:res_lstm}). The information-theoretic reading is that the tip under-identifies the internal configuration of a two-segment arm, and that the mid marker supplies a partial observation of that configuration, narrowing the set of pressures consistent with the observation. Temporal context alone does \emph{not} produce this effect---the tip-only recurrent network is marginally worse than the tip-only feed-forward network on Dataset 1---yet the combination of mid-marker input and temporal context is better than either alone. The likely reason is that the recurrent network extracts the trajectory of the internal configuration rather than the trajectory of the tip, and that trajectory is a meaningful feature only once the internal configuration is itself observable. This is consistent with the hysteresis hypothesis that motivated Experiment~3 but narrows it: history helps when the features it has access to are informative enough for the history to carry physical meaning, and does not help when those features under-identify the system.

\paragraph{The Fang 2022 trade-off is a property of multi-valued inverses, not of neural architectures.}
The Jacobian-learned iteration of Section~\ref{sec:res_jacobian} was predicted to sacrifice pressure-space accuracy for position-space accuracy on a multi-valued inverse, on the grounds that direct regression averages valid solutions while a Jacobian iteration commits to one \cite{fang2022jacobian}. The measured trade-off on Dataset 2 was a $33\%$ increase in pressure MAE against a $71\%$ reduction in closed-loop position error (Table~\ref{tab:res_jacobian_pos}), and on Dataset 1 the trade-off is similar in sign but smaller in magnitude. The same reversal would be expected for any pair of methods that chooses posterior-mean versus posterior-mode behaviour on a multi-valued inverse, regardless of their implementation details. The practical consequence is that Jacobian iteration and direct regression are not ranked on a single ``quality'' scale but instead measure different objectives: a controller that places the tip in a desired position should adopt a Jacobian iteration; a system that reports pressure MAPE to a downstream consumer should adopt direct regression.

\paragraph{Sim-to-real transfer is dominated by workspace geometry, not by sample count.}
The negative sim-to-real result on Dataset 2 (Section~\ref{sec:res_sim_transfer}) is worth reading with care. Strategy~B---sim pre-training followed by real fine-tuning---matched Strategy~A at every real-data budget tested, including the $10\%$ budget at which one would most expect a prior to help. Strategy~C, which used only synthetic data, failed with $R^{2} = -1.71$ on Dataset 2. The synthetic workspace produced by the calibrated rod of Section~\ref{sec:res_sim_cal} was five times the linear extent of Dataset 2 on every axis and centred in a different region of tip space, so the pre-training distribution had almost no overlap with the test distribution. Under these conditions the pre-training is not a prior toward the right answer but a prior toward an irrelevant answer, and fine-tuning on a small fraction of real data discards most of the pre-trained weights. The more general reading is that a Manti-inspired rod is not a drop-in prior for an undocumented robot: the value of any such prior is bounded above by the overlap between its synthetic workspace and the real operating region, and the $720$-sample count that limited the present simulation is not the reason this ceiling was hit.

\paragraph{A compact decision criterion.}
Across the six methods and two datasets tested here, the right inverse-kinematics method for a new pneumatic soft-robot arm is determined less by the method itself than by three properties of the arm and its intended use. First, actuation redundancy: if there are more actuation degrees of freedom than tip degrees of freedom, a Jacobian-based iteration can be expected to drive closed-loop error to the tolerance floor, as observed on the six-chamber Dataset 1 arm. Second, sensing coverage: if the tip under-identifies the internal configuration, additional observations---mid markers, full-shape sensing, or any other partial observation of the internal state---lift the predictive ceiling more than any network-architecture change, as observed on Dataset 1 with the mid-marker ablation. Third, target-domain overlap: a simulator prior is useful only when its synthetic workspace covers the intended operating region of the real arm; otherwise the most data-efficient route is still to train on real data alone, as observed on Dataset 2. These three properties together recover most of the regime-specific findings of Chapter~\ref{chap:results} and give a compact decision criterion that does not privilege any single method.

\section{Comparison with published baselines}
\label{sec:disc_benchmarks}

This section positions the headline numerical findings of Chapter~\ref{chap:results} against three reference points in the soft-robotics inverse-kinematics literature: Ma et al.\ \cite{ma2022bp} on a purpose-built three-chamber pneumatic arm, Giorelli et al.\ \cite{giorelli2015nn} on a cable-driven soft arm, and Fang et al.\ \cite{fang2022jacobian} on a pneumatic platform with sim-to-real learning. Each reference uses a different robot, dataset, and metric, so the comparisons below are organised to separate what can be compared directly from what requires cross-platform caveats. Table~\ref{tab:disc_benchmarks} collects the position-space tracking numbers for the references and for the two best configurations of this thesis, normalised by the rest length of each arm so that absolute millimetres are not the only scale.

\begin{table}[ht]
\centering
\caption[Position-space tracking error against published baselines.]{Position-space tracking error for three published reference points and the two best configurations of this thesis, normalised by the rest length of the corresponding arm. The $280$\,mm length is taken from Giorelli et al.\ \cite{giorelli2015nn}; the $410$\,mm assembled length of the I-Support arm is taken from Manti et al.\ \cite{manti2016soft}; the $100$\,mm workspace of Dataset 2 is the observed tip span after preprocessing (Section~\ref{sec:eda_ds2}) and is used in lieu of a documented arm length. Entries marked ``---'' indicate metrics that the source did not report in directly comparable form.}
\label{tab:disc_benchmarks}
\begin{tabularx}{\linewidth}{@{}l X X X X@{}}
\toprule
Source                                              & Platform                      & Method                      & Mean error (mm)   & Relative (\%\,of $L$) \\
\midrule
Giorelli et al.\ \cite{giorelli2015nn}               & $280$\,mm cable arm            & Analytical Jacobian          & $15.12$             & $5.40$                  \\
Giorelli et al.\ \cite{giorelli2015nn}               & $280$\,mm cable arm            & Feed-forward network         & $7.35$              & $2.62$                  \\
Fang et al.\ \cite{fang2022jacobian}                 & pneumatic soft robot           & Learned-Jacobian iteration   & sub-mm              & ---                     \\
This thesis, Dataset 1 (Section~\ref{sec:res_traj})   & $410$\,mm I-Support            & Jacobian-learned iteration   & $0.77$              & $0.19$                  \\
This thesis, Dataset 2 (Section~\ref{sec:res_traj})   & $\sim\!100$\,mm three-valve arm & Sim-to-real feed-forward     & $6.20$              & $6.20$                  \\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Gap to the Ma et al.\ benchmark.}
Ma et al.\ \cite{ma2022bp} reported a pressure-space MAPE below $5\%$ on a three-chamber pneumatic arm with $216$ training samples; the best-performing Dataset 2 configuration in this thesis reaches $34.4\%$ MAPE with $12{,}650$ training samples (Section~\ref{sec:res_mlp_tip}). The $6.9\times$ gap is not attributable to dataset size, since the thesis dataset is about sixty times larger than Ma's, and it is most likely attributable to three differences in the experimental conditions. First, Ma's arm was purpose-built for the experiment with documented actuation units (bar) and a documented straight-pose origin, whereas the Dataset 2 commands are integer-valued on an undocumented scale $[0, 2500]$ and the recorded origin sits near the straight-pose tip rather than the robot base (Section~\ref{sec:res_pcc_single}). Second, Ma's dataset was recorded in quasi-static sampling mode, whereas the Dataset 2 rows are IID random actuations (Section~\ref{sec:eda_ds2}) in which stochastic hysteresis contributes directly to the residual. Third, the feed-forward network used here has no hysteresis term; the recurrent extension of Section~\ref{sec:res_lstm} shifted Dataset 2 MAPE from $34.5\%$ to $34.2\%$, a small improvement consistent with Dataset 2's weak temporal structure rather than with the absence of a physical hysteresis effect. A tighter comparison against Ma's benchmark would require either a recalibrated Dataset 2 in documented units or a Ma-style purpose-built recording with the random-walk protocol used here.

\paragraph{Feed-forward network beats the analytical baseline, as predicted by Giorelli.}
Giorelli et al.\ \cite{giorelli2015nn} compared a feed-forward inverse-statics network against an analytical Jacobian iteration on a $280$\,mm cable-driven soft arm and reported a $2.05\times$ accuracy advantage for the network ($7.35$\,mm mean error against $15.12$\,mm) together with a $2160\times$ speed advantage at inference. The Dataset 2 results of this thesis reproduce the ordering: the tip-only feed-forward network reached pressure MAE $150.1$ against $285.2$ on the reachable subset of the single-segment PCC baseline (Section~\ref{sec:res_mlp_tip}), a $1.9\times$ advantage that matches Giorelli's $2.0\times$ figure within reading accuracy. In position-space terms, the Dataset 2 trajectory-tracking error of $7.40$\,mm on an approximately $100$\,mm workspace corresponds to $7.4\%$ relative error against Giorelli's $2.62\%$ on their $280$\,mm arm. The difference in relative error is consistent with the undocumented-units and zero-frame issues above, and with the smaller absolute workspace of Dataset 2 placing any fixed measurement noise proportionally closer to the target.

\paragraph{Jacobian-learned IK reaches the sub-millimetre level reported by Fang et al.}
Fang et al.\ \cite{fang2022jacobian} introduced the learned-Jacobian inverse-kinematics scheme adopted in Section~\ref{sec:jacobian} and reported sub-millimetre mean tracking error on their pneumatic soft-robot platform. The Dataset 1 configuration of Section~\ref{sec:res_jacobian} reached a mean closed-loop position error of $0.71$\,mm on the per-sample test sub-sample and $0.77$\,mm on the reference trajectories of Section~\ref{sec:res_traj}, which is at the same order of magnitude as Fang's result on a different physical platform. This is one of the cleaner literature replications available in the thesis, and it suggests that the Fang method generalises to the random-walk pressure regime of Dataset 1 without special tuning beyond the reference hyperparameters reported by the original authors. The sim-to-real extension that is Fang's main contribution was not validated here: the negative transfer result of Section~\ref{sec:res_sim_transfer} shows only that a Manti-inspired rod does not serve as a useful pre-training prior for the undocumented Dataset 2 robot, and a different simulator whose workspace matches Dataset 2 could still recover Fang's sim-to-real benefit.

\paragraph{Limits of cross-platform comparison.}
None of the three reference works above uses exactly the same robot, dataset, or metric as this thesis, so the numerical comparisons are indicative rather than definitive. Ma's arm has a different geometry and known actuation units; Giorelli's arm is cable-driven and its task is planar; Fang's platform is a different pneumatic robot with documented calibration and sim-to-real infrastructure. The three reference numbers are therefore best read as scale markers for what is achievable on cleanly characterised soft-robot platforms, rather than as targets that the present thesis is expected to hit on Dataset 2. The quantitative claim that survives these caveats is the one supported by the cleaner Dataset 1 geometry: that the warm-started Jacobian-learned iteration reaches sub-millimetre closed-loop error on a real two-segment pneumatic recording, matching Fang's sub-millimetre order of magnitude and improving by roughly an order of magnitude against the analytical-Jacobian accuracy that Giorelli reported on a simpler platform.

\section{The role of mid-segment information on Dataset 1}
\label{sec:disc_mid}

\paragraph{Why tip-only IK is under-determined on a two-segment arm.}
The I-Support manipulator of Dataset 1 has six chamber pressures and a three-dimensional tip. Even under the piecewise-constant-curvature model, a given tip position can be reached by a continuous one-parameter family of per-segment configurations: the first segment can bend more and the second less, or the reverse, with the tip held fixed. The inverse is therefore not a function but a relation, and a supervised regressor trained with a squared-error loss will gravitate toward the training-distribution posterior mean over the valid configurations for each tip, as argued in Section~\ref{sec:disc_interpretation}. The tip-only feed-forward network of Section~\ref{sec:res_mlp_tip} plateaued at $R^{2} = 0.32$ after four training epochs, and two of its six per-chamber predictions sat at or below chance ($R^{2} = -0.03$ for chamber 2 and $0.04$ for chamber 4 in Fig~\ref{fig:res_mlp_pred_vs_actual}). Both observations are consistent with the network having learned what it can learn and the remaining variance belonging to the posterior itself rather than to model capacity.

\paragraph{Mid-marker input concentrates the posterior.}
Augmenting the input with the mid-marker coordinates adds three partial observations of the internal configuration of the arm---specifically, the centroid of the three mid-section markers (Section~\ref{sec:ds1}). In the PCC reading of this geometry those three coordinates identify the end of the first segment, which, together with the tip position, over-determines the per-segment bending angle, azimuth, and elongation to within optical marker noise. The feed-forward ablation of Section~\ref{sec:res_mlp_tipmid} measured the effect: pressure MAE fell from $20.13$ to $17.05$ (a $15\%$ reduction), $R^{2}$ rose from $0.32$ to $0.44$, and the Wilcoxon signed-rank test on the paired per-frame errors returned $p \approx 2.4 \times 10^{-211}$. For a change in input features with no change in architecture or training protocol (Section~\ref{sec:mlp_tipmid}), this is an unusually clean intervention: whatever improvement the mid marker provides is attributable to information content rather than to model capacity or training-loop differences.

\paragraph{Temporal context amplifies the mid-marker signal rather than replacing it.}
A notable asymmetry emerged in the recurrent experiments of Section~\ref{sec:res_lstm}. The tip-only LSTM was marginally worse than the tip-only feed-forward network on Dataset 1 (per-frame RMSE $24.88$ against $24.07$; $R^{2} = 0.28$ against $0.32$), indicating that a ten-frame trajectory of tip-only observations carries no additional information that a regressor can use. The same LSTM applied to tip-plus-mid input produced $R^{2} = 0.51$, a $17\%$ relative improvement over the feed-forward tip-plus-mid configuration. The most straightforward reading is that the recurrent network is extracting the trajectory of the internal configuration rather than the trajectory of the tip. Historical tip samples alone have no unique mapping to historical internal configuration and therefore remain under-determined; once the mid-marker coordinates pin the internal state at every frame, the history of that state becomes a physically meaningful feature whose temporal derivatives carry velocity and hysteresis information. Temporal modelling of soft-robot inverse kinematics is therefore not a universally useful architectural choice: its benefit is contingent on the input features already resolving the under-identification that would otherwise corrupt the time series.

\paragraph{What the ablation implies for sensor placement.}
The headline number---pressure $R^{2}$ rising from $0.32$ to $0.51$ when three additional marker coordinates are added to the input---is a strong argument for investing in internal-shape sensing rather than deeper networks or larger training sets on multi-segment soft arms. One triad of markers at the inter-module interface increased the Dataset 1 $R^{2}$ by more than any architectural change tested in this thesis. It is worth reading this result as an observation about the physics of the task, not only about the network: on a two-segment arm, six chamber pressures are twice as many as the three tip coordinates, and the factor-of-two over-parameterisation is exactly what the mid-marker triad cancels by supplying three additional observations of the internal state. A practitioner designing a new two-segment soft arm and choosing between ``deeper inverse-kinematics network'' and ``add one mid-body marker triad'' would, on these data, find the marker cheaper and more informative than any network-side intervention.

\paragraph{Caveats and generalisation.}
Two caveats limit the strength of the conclusion. First, only one internal observation was tested; it is not established from the present data whether adding further markers along the arm would continue to concentrate the posterior or whether the mid-marker triad already captures most of the available gain. Second, the trajectory-tracking evaluation of Section~\ref{sec:res_traj} exposed a failure mode of the mid-marker network when deployed without a mid signal: the evaluation protocol substituted the training-distribution centroid for the mid coordinates, and the network---which had been trained on a distribution in which tip and mid co-vary---extrapolated off that slice and reached $19.08$\,mm mean tracking error, the worst Dataset 1 result in Table~\ref{tab:res_overall_ds1}. The mid-marker benefit is therefore contingent on a real mid-marker signal being available at deployment time, and any control system intending to use a tip-plus-mid network would need a running shape estimator of comparable quality. A more general claim---that partial-shape observation resolves multi-segment IK under-determination regardless of platform---would require a further study on an arm with a different module count, or with a different shape-sensor modality such as fibre-Bragg-grating fibres or embedded proprioceptive cells, which is beyond the scope of this thesis.

\section{Temporal modelling and hysteresis}
\label{sec:disc_temporal}

\paragraph{Where temporal modelling helped, and where it did not.}
The recurrent networks of Section~\ref{sec:res_lstm} were predicted to capture hysteresis and other history-dependent effects that a feed-forward network cannot see. The prediction was borne out in only one of the three configurations tested. The tip-plus-mid recurrent network on Dataset 1 improved on its feed-forward counterpart by a modest but statistically clean margin ($R^{2} = 0.51$ against $0.44$). The tip-only recurrent network on Dataset 1 was marginally worse than the tip-only feed-forward network, and the tip-only recurrent network on Dataset 2 was marginally better ($4.8\%$ MAE reduction). On smooth reference trajectories the recurrent and feed-forward networks were indistinguishable within about a millimetre on every trajectory of either dataset (Section~\ref{sec:res_traj}). Temporal modelling therefore did not deliver the uniform benefit one would expect if hysteresis were the dominant residual error on these datasets.

\paragraph{Why Dataset 1 tip-only does not benefit from history.}
A $K$-frame window of past inputs only helps a regressor when those past inputs constrain the mapping from the current input to the current output. On the two-segment Dataset 1 arm, the mapping from tip position to chamber pressure is multi-valued (Section~\ref{sec:disc_mid}), and the tip velocity across the window does not identify which branch of the solution manifold the current configuration lies on, because the per-segment bending split is not observable from the tip alone. The recurrent network therefore receives a richer input with no accompanying reduction in under-identification, and its optimisation collapses to the same posterior mean that the feed-forward network already reached. The window-length sweep of Section~\ref{sec:res_lstm} exposed the symptom cleanly: the best-epoch index of the tip-only recurrent training run fell from $15$ at $K = 5$ to $1$ at $K = 20$, so longer windows accelerated over-fitting rather than recovering signal.

\paragraph{Why Dataset 1 tip-plus-mid does benefit.}
Once the mid-marker coordinates pin the internal state at every frame (Section~\ref{sec:disc_mid}), the $K$-frame window of those coordinates carries the velocity and acceleration of the internal state, and therefore the direction in which the arm is currently moving. For a hysteretic system the mapping from configuration to pressure depends on the recent history of configuration, not only on the current configuration, and the tip-plus-mid recurrent network is the only configuration tested that has access to the features it would need to read that history. The magnitude of the improvement ($R^{2}$ from $0.44$ to $0.51$) suggests that a hysteretic signal is indeed present in Dataset 1, though not enough to dominate the remaining residual error. A cleaner test would require either a dataset with a more structured temporal protocol than the random-walk excitation used here, or an explicit hysteresis parametrisation (for example a Preisach or Bouc-Wen model) fit to the recovered internal-state trajectory.

\paragraph{Why Dataset 2 benefits only marginally.}
Dataset 2 behaves as an IID sample set (Section~\ref{sec:eda_ds2}); the step-size ratio of $1.00$ in valve-command space and $0.96$ in tip space leaves almost no temporal structure for a recurrent network to exploit. The $4.8\%$ MAE reduction from the recurrent model is unlikely to reflect captured hysteresis, since there is no temporal order in which hysteresis could manifest. A more plausible explanation is that windowing acts as a mild regulariser: adjacent rows with similar tip positions supply a local averaging signal that reduces per-sample prediction variance. The monotone benefit with longer $K$ (MAE $148.05 \to 138.68$ between $K = 5$ and $K = 20$) is consistent with a regularisation story rather than with a temporal-signal story. For a classifier on truly IID data longer windows would not be expected to help at all; the small positive gradient here is plausibly attributable to residual spatial adjacency between neighbouring rows in the recording order rather than to time.

\paragraph{On smooth trajectories the recurrent benefit is smoothness, not accuracy.}
The trajectory-tracking evaluation of Section~\ref{sec:res_traj} showed no meaningful accuracy difference between the recurrent and feed-forward networks on any of the four reference trajectories of either dataset. At the tracking scale tested---safe-radius trajectories of $7$--$12$\,mm traced over $120$ target points---the current tip already carries most of the information that the past window could supply, because successive targets are close enough to be essentially the same sample from the network's point of view. Where the recurrent network did win was on pressure-command smoothness: its sum-of-squared discrete third differences on Dataset 1 was one to two orders of magnitude below those of the feed-forward networks. The hidden-state memory acts as a first-order low-pass filter on the pressure output, which is a property of the architecture rather than a learned feature. For a closed-loop controller that cares about actuator wear, valve response, or energy consumption, the recurrent network remains worth keeping on those grounds, even at equivalent tracking accuracy.

\paragraph{Limits of the tested sequence lengths.}
Three window lengths were compared ($K \in \{5, 10, 20\}$), and the primary configuration $K = 10$ is near the upper end of what was practically useful on Dataset 1: the best-epoch collapse at $K = 20$ shows that the recurrent network was unable to make use of longer windows under the random-walk excitation of the dataset. The absence of full-recording attention models and of explicit hysteresis parametrisations means that this thesis does not establish an absolute ceiling on what temporal modelling could extract from Dataset 1. What it does establish is that, within the architecture and input-feature space of Section~\ref{sec:lstm}, window lengths up to $20$ frames do not extract further signal beyond what the primary $K = 10$ configuration recovers, and that the benefit of temporal modelling on soft-robot inverse kinematics is contingent on the input features already resolving the multi-valued-inverse under-identification that would otherwise corrupt the time series.

\section{Jacobian-learned iteration versus direct regression}
\label{sec:disc_jacobian_vs_mlp}

\paragraph{Recap: position-space winner versus pressure-space winner.}
The Fang 2022 trade-off summarised in Section~\ref{sec:disc_interpretation} gives one global statement of the choice between the two method families: a direct-regression network is the posterior-mean estimator over valid pressure solutions and therefore minimises pressure-space squared error, while a Jacobian iteration commits to a single posterior-mode solution near its initialisation and therefore minimises position-space error at inference. The present section decomposes that global trade-off into four operational axes---generalisation, sample efficiency, out-of-distribution robustness, and runtime---on which the choice has to be made in practice.

\paragraph{Generalisation is bounded by different things.}
A direct-regression network's generalisation to novel tip targets is bounded by the coverage of the training distribution in tip space: any target outside the convex hull of training tips is extrapolation, and the network's prediction has no formal guarantee. A Jacobian iteration's generalisation is bounded instead by the accuracy of the forward-kinematics network on the iterates that the solver visits during the solve. Two consequences follow. First, the Jacobian method inherits the failure modes of the forward network: a forward network with test-set MAE of $2.55$\,mm on Dataset 2 (Section~\ref{sec:res_jacobian}) caps the Jacobian iteration's achievable closed-loop error at roughly that scale, regardless of how many iterations are spent. Second, the Jacobian method can in principle reach targets outside the inverse-kinematics training distribution, because its search is anchored by the forward model rather than by a learned inverse map. The sub-millimetre result on the Dataset 1 per-sample test sub-sample---where the seed-0 test trajectories were never seen at training time---is a partial demonstration: the warm-started iteration hit the tolerance floor on every target in the sub-sample (Table~\ref{tab:res_jacobian_pos}).

\paragraph{Sample efficiency favours the Jacobian formulation in principle.}
A direct-regression inverse-kinematics network has to learn a map across the full target distribution, and its sample complexity scales with the complexity of that distribution. A Jacobian iteration only needs its forward-kinematics network to fit the pressure-to-position map, which is single-valued regardless of actuation redundancy and is therefore cheaper to learn: the Dataset 1 forward network reached test MAE $12.97$\,mm after only two epochs (Section~\ref{sec:res_jacobian}), against the four epochs required by the tip-only inverse network of Section~\ref{sec:res_mlp_tip}. In the limit of small real-data budgets this argument suggests a Jacobian iteration would remain usable where a direct-regression network over-fits; a systematic data-efficiency sweep of the Jacobian method on a downsampled real-data budget is a natural extension of the sim-to-real study of Section~\ref{sec:res_sim_transfer} and would quantify the above intuition.

\paragraph{Out-of-distribution robustness is the honest cost of the Jacobian iteration.}
The clearest out-of-distribution example in the thesis is the Dataset 2 helical spiral of Section~\ref{sec:res_traj}: its targets trace the full $z$-half-extent of the observed workspace and include configurations close to the edge of what the forward-kinematics network represents reliably. The warm-started Jacobian iteration reached a mean tracking error of $31.94$\,mm on the spiral, with a worst-case single-target error of $258$\,mm, compared with $7.42$\,mm and $8.17$\,mm for the sim-to-real and tip-only feed-forward networks on the same trajectory. A single diverging DLS run on a three-valve arm is enough to invalidate the mean because there is no actuation redundancy available to recover from a poor update; feed-forward methods cannot exhibit this behaviour because a single forward pass cannot diverge. Operationally, this means the Jacobian iteration's sub-millimetre advantage is conditional on staying inside the workspace where the forward-kinematics network is well-conditioned, and it will fail gracefully only when explicit workspace bounds or iteration-based safety checks are added to the solver.

\paragraph{Runtime argues against the Jacobian iteration at high control rates.}
The warm-started Jacobian iteration cost about $46$\,ms per target (Section~\ref{sec:res_jacobian}) against well under $1$\,ms for the feed-forward networks, two to three orders of magnitude slower depending on the measurement context. Both sit inside a $50$\,ms budget appropriate for a $20$\,Hz controller, but neither the Jacobian method nor any other method tested here would meet a $1$\,kHz control loop on the available hardware. The bulk of the Jacobian cost is the reverse-mode automatic differentiation of the forward network and the linear solve at each step (Eq.~\eqref{eq:dls_step}); runtime therefore scales linearly with the number of iterations and with the width of the forward network, not with the dataset size or the target's distance from the training distribution. The implication for practical soft-robot controllers is regime-dependent. For quasi-static pick-and-place tasks the Jacobian iteration's accuracy gain is the dominant consideration and the latency is irrelevant. For dynamic tasks that require closed-loop updates at kilohertz rates the feed-forward methods remain the only candidate unless the forward-network evaluation can be specialised (for example, to a fixed configuration neighbourhood in which a Taylor expansion replaces the autograd Jacobian).

\paragraph{The hybrid is already the default.}
The warm-started configuration adopted for every headline Jacobian number in Chapter~\ref{chap:results} is itself a hybrid: the direct-regression network's prediction supplies the initial pressure $\mathbf{p}_0$, and the DLS iteration then refines it. On Dataset 2, warm-starting reduced the mean iteration count from $16.8$ (zero initialisation) to $2.3$ and lifted the convergence rate from $78\%$ to $99\%$ (Section~\ref{sec:res_jacobian}). The hybrid inherits the direct network's broad-coverage generalisation from the zero initialisation and the Jacobian iteration's sub-millimetre refinement at inference. Three natural extensions of this hybrid would test its limits: a nearest-neighbour initialisation from a cached training distribution, a learned stopping criterion that exits the iteration before divergence, and a two-stage controller that runs the feed-forward network in a fast inner loop and invokes the Jacobian iteration only when the position error exceeds a threshold. None of these were tested in the present thesis and all are compatible with the architectures and checkpoints of Chapter~\ref{chap:methods}.

\section{Sim-to-Real Observations}
% TODO: what transfers, what does not; the reality gap for pneumatic
%       actuation dynamics.

\section{Limitations}

\subsection{No Physical Robot}
% TODO: all evaluation is on pre-recorded datasets; no closed-loop testing.

\subsection{Dataset Coverage}
% TODO: two datasets, two geometries; broader generalisation unverified.

\subsection{Simulator Fidelity}
% TODO: PyElastica limitations (no air dynamics, simplified hysteresis).

\subsection{Metric Choice}
% TODO: tip-only error ignores shape; implications for downstream use.

\section{Implications}
% TODO: practical take-aways for practitioners choosing an IK strategy
%       for a new pneumatic soft arm.
